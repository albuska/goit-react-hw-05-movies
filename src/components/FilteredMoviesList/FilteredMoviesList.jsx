const FilteredMoviesList = moviesList => {
  console.log(moviesList);
  return (
    <div>
      {/* <ul>
        {results.map(result => (
          <li key={result.id}>
            <p>{result.title}</p>
          </li>
        ))}
      </ul> */}
    </div>
  );
};

export default FilteredMoviesList;
